version: 0.2

env:
  variables:
    ENV: "${env}"
phases:
  pre_build:
    commands:
    - git branch
    - cd _infra/aws/$ENV
    - terraform init
    - terraform fmt -check
  build:
    commands:
    - terraform apply -auto-approve
